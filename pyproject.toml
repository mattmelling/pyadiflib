[project]
name = "pyadiflib"
version = "0.0.1"
requires-python = ">= 3.8"
authors = [
    { name = "Matt G4IYT", email = "matt@g4iyt.uk" }
]
readme = "README.md"

[project.optional-dependencies]
dev = [
    "pytest"
]